version: '3.3'

services:
  greeting:
    container_name: greeting
    image: greeting:latest
    build: .
    ports:
      - "8080:8080"

  proxy:
    container_name: proxy
    image: proxy:latest
    build: proxy
    ports:
      - "8180:80"